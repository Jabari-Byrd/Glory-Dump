[package]
name = "glory-dump-core"
version = "3.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Deterministic, allocation-free rules for the GLORY/DUMP game"

[lib]
crate-type = ["lib"]

[features]
default = []
test-fast = []
